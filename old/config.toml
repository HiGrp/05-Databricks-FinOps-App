# ============================================================
# FinOps Optimizer - config dev local (dist/ a son propre config.toml)
# ============================================================

[app]
# true = DuckDB fake data | false = system tables Databricks
dev_mode = true
[license]
enabled = true
trial_days = 7
